"use client";

import Link from "next/link";
import { ArrowLeft, ArrowRight, Search } from "lucide-react";
import { useMemo, useState } from "react";
import { formatVnd, products } from "@/lib/products";

export default function ProductsPage() {
  const [query, setQuery] = useState("");
  const [type, setType] = useState<"all" | "bouquet" | "basket">("all");
  const [sort, setSort] = useState("featured");
  const filtered = useMemo(() => products.filter((product) => product.status === "published" && (type === "all" || product.type === type) && (!query.trim() || `${product.name} ${product.sku} ${product.categories.join(" ")}`.toLowerCase().includes(query.trim().toLowerCase()))).sort((a, b) => sort === "low" ? (a.salePrice ?? a.price) - (b.salePrice ?? b.price) : sort === "high" ? (b.salePrice ?? b.price) - (a.salePrice ?? a.price) : sort === "newest" ? b.sourceDate.localeCompare(a.sourceDate) : Number(Boolean(b.featured)) - Number(Boolean(a.featured))), [query, sort, type]);
  return <main className="min-h-screen bg-background"><header className="border-b border-border bg-background/95"><div className="container-cas flex h-16 items-center justify-between gap-4"><Link href="/" className="font-display text-2xl">CÁ&apos;S HOA</Link><Link href="/" className="flex items-center gap-1 text-sm font-semibold text-primary"><ArrowLeft size={15} /> Trang chủ</Link></div></header><section className="container-cas py-8 sm:py-12"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-primary">Cửa hàng CÁ&apos;S HOA</p><h1 className="mt-2 font-display text-5xl">Tất cả sản phẩm.</h1><p className="mt-3 max-w-xl text-sm leading-7 text-muted-foreground">Bó hoa và giỏ hoa được shop cập nhật theo mẫu có thật. Giá và tình trạng sẽ được shop xác nhận trước khi chuẩn bị.</p><div className="mt-7 grid gap-3 sm:grid-cols-[1fr_auto_auto]"><label className="flex items-center gap-2 rounded-full border border-border bg-surface px-4 py-3"><Search size={17} className="text-muted-foreground" /><span className="sr-only">Tìm sản phẩm</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Tìm tên hoa, mã sản phẩm..." className="min-w-0 flex-1 bg-transparent text-sm outline-none" /></label><select aria-label="Lọc loại sản phẩm" value={type} onChange={(event) => setType(event.target.value as typeof type)} className="rounded-full border border-border bg-surface px-4 py-3 text-sm font-semibold"><option value="all">Tất cả loại</option><option value="bouquet">Bó hoa</option><option value="basket">Giỏ hoa</option></select><select aria-label="Sắp xếp sản phẩm" value={sort} onChange={(event) => setSort(event.target.value)} className="rounded-full border border-border bg-surface px-4 py-3 text-sm font-semibold"><option value="featured">Nổi bật</option><option value="newest">Mới nhất</option><option value="low">Giá thấp đến cao</option><option value="high">Giá cao đến thấp</option></select></div><div className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{filtered.map((product) => <Link href={`/san-pham/${product.slug}`} key={product.id} className="group"><div className="overflow-hidden rounded-2xl bg-surface-muted"><img src={product.image} alt={product.name} className="aspect-square w-full object-cover transition duration-500 group-hover:scale-105" /></div><p className="mt-3 font-display text-xl leading-tight">{product.name}</p><p className="mt-1 text-sm font-bold text-primary">{formatVnd(product.salePrice ?? product.price)}</p><p className="mt-1 text-xs text-muted-foreground">{product.type === "bouquet" ? "Bó hoa" : "Giỏ hoa"} · {product.sku}</p></Link>)}</div>{!filtered.length && <div className="mt-10 rounded-2xl bg-surface p-10 text-center"><p className="font-display text-2xl">Chưa tìm thấy sản phẩm</p><p className="mt-2 text-sm text-muted-foreground">Thử lại với từ khóa hoặc bộ lọc khác.</p></div>}<Link href="/tra-cuu-don-hang" className="mt-10 inline-flex items-center gap-2 text-sm font-bold text-primary">Tra cứu đơn hàng <ArrowRight size={15} /></Link></section></main>;
}
