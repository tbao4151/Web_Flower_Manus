"use client";

import { FormEvent, useState } from "react";
import { ArrowLeft, Clipboard, Leaf, LoaderCircle, Search } from "lucide-react";
import Link from "next/link";

type Result = { order_code: string; status: string; recipient_name: string; delivery_address: string; total_vnd: number; created_at: string; order_items: { product_name_snapshot: string; quantity: number; line_total_vnd: number }[] };
const statusLabels: Record<string, string> = { pending_confirmation: "Chờ xác nhận", confirmed: "Đã xác nhận", preparing: "Đang chuẩn bị", delivering: "Đang giao", completed: "Hoàn tất", cancelled: "Đã huỷ" };
const money = (n: number) => new Intl.NumberFormat("vi-VN").format(n);

export default function LookupOrderPage() {
  const [orderCode, setOrderCode] = useState(""); const [phone, setPhone] = useState(""); const [result, setResult] = useState<Result | null>(null); const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setError(""); setResult(null); const params = new URLSearchParams({ orderCode: orderCode.trim(), recipientPhone: phone.trim() }); const response = await fetch(`/api/orders?${params.toString()}`); const data = await response.json().catch(() => ({})); setLoading(false); if (!response.ok) { setError(data.error || "Không tìm thấy đơn hàng."); return; } setResult(data.order); }
  return <main className="min-h-screen bg-background px-5 py-6 sm:px-8"><div className="mx-auto max-w-2xl"><header className="flex items-center justify-between border-b border-border pb-5"><Link href="/" className="flex items-center gap-2"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-primary text-white"><Leaf size={17} /></span><span className="font-display text-xl">CÁ&apos;S HOA</span></Link><Link href="/dang-nhap" className="text-sm font-semibold text-primary">Đăng nhập</Link></header><section className="mt-10 rounded-[26px] border border-border bg-surface p-6 sm:p-8"><Clipboard className="text-primary" /><h1 className="mt-3 font-display text-4xl">Tra cứu đơn hàng</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Nhập mã đơn và số điện thoại người nhận để xem nhanh trạng thái. Hệ thống chỉ hiển thị thông tin tối thiểu để bảo vệ đơn hàng.</p><form onSubmit={submit} className="mt-7 grid gap-4 sm:grid-cols-[1fr_1fr_auto] sm:items-end"><label className="text-sm font-semibold">Mã đơn<input value={orderCode} onChange={(event) => setOrderCode(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-border bg-background px-4 uppercase" placeholder="CSH-2026-..." required /></label><label className="text-sm font-semibold">Số điện thoại<input value={phone} onChange={(event) => setPhone(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-border bg-background px-4" inputMode="tel" placeholder="0356 925 367" required /></label><button disabled={loading} className="flex h-12 items-center justify-center gap-2 rounded-full bg-primary px-5 font-bold text-white disabled:opacity-60">{loading ? <LoaderCircle className="animate-spin" size={17} /> : <Search size={17} />} Tra cứu</button></form>{error && <p role="alert" className="mt-5 rounded-xl bg-[#fae8e4] p-4 text-sm text-danger">{error}</p>}{result && <article className="mt-7 rounded-2xl border border-border bg-background p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-bold">{result.order_code}</p><p className="mt-1 text-xs text-muted-foreground">{new Date(result.created_at).toLocaleString("vi-VN")}</p></div><span className="rounded-full bg-[#e4ecdf] px-3 py-1 text-xs font-bold text-primary">{statusLabels[result.status] || result.status}</span></div><p className="mt-5 text-sm"><strong>Người nhận:</strong> {result.recipient_name}</p><p className="mt-1 text-sm"><strong>Địa chỉ:</strong> {result.delivery_address}</p><div className="mt-5 space-y-2 border-t border-border pt-4 text-sm">{result.order_items.map((item, index) => <div key={index} className="flex justify-between"><span>{item.product_name_snapshot} × {item.quantity}</span><strong>{money(item.line_total_vnd)}đ</strong></div>)}<div className="flex justify-between border-t border-border pt-3 text-base"><strong>Tổng tạm tính</strong><strong className="text-primary">{money(result.total_vnd)}đ</strong></div></div></article>}</section><Link href="/" className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-primary"><ArrowLeft size={15} /> Về cửa hàng</Link></div></main>;
}
