"use client";

import { FormEvent, useState } from "react";
import { ArrowLeft, Clipboard, LoaderCircle, Search } from "lucide-react";
import Link from "next/link";

type Result = {
  order_code: string; status: string; recipient_name: string; is_pickup: boolean; delivery_address: string | null; delivery_date: string | null; delivery_time: string | null;
  total_vnd: number; subtotal_vnd: number; shipping_vnd: number; shipping_fee_confirmed: boolean; deposit_required_vnd: number; deposit_paid_vnd: number; remaining_amount_vnd: number;
  payment_status: string; delivery_status: string; carrier_name: string | null; shipper_name: string | null; estimated_delivery_at: string | null; created_at: string;
  order_items: { product_name_snapshot: string; quantity: number; line_total_vnd: number }[];
};
const statusLabels: Record<string, string> = { pending_confirmation: "Chờ xác nhận", confirmed: "Đã xác nhận", preparing: "Đang chuẩn bị", ready: "Sẵn sàng", delivering: "Đang giao", completed: "Hoàn tất", cancelled: "Đã huỷ" };
const deliveryLabels: Record<string, string> = { pending: "Chờ xử lý", assigned: "Đã phân công", out_for_delivery: "Đang giao", delivered: "Đã giao", pickup_ready: "Sẵn sàng nhận", picked_up: "Đã nhận tại shop", failed: "Giao không thành công" };
const paymentLabels: Record<string, string> = { unpaid: "Chưa thanh toán", partially_paid: "Đã thanh toán một phần", paid: "Đã thanh toán" };
const money = (n: number) => new Intl.NumberFormat("vi-VN").format(n);

export default function LookupOrderPage() {
  const [orderCode, setOrderCode] = useState(""); const [phone, setPhone] = useState(""); const [result, setResult] = useState<Result | null>(null); const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setError(""); setResult(null); const params = new URLSearchParams({ orderCode: orderCode.trim(), recipientPhone: phone.trim() }); const response = await fetch(`/api/orders?${params.toString()}`); const data = await response.json().catch(() => ({})); setLoading(false); if (!response.ok) { setError(data.error || "Không tìm thấy đơn hàng."); return; } setResult(data.order); }
  return <main className="min-h-screen bg-background px-5 py-6 sm:px-8"><div className="mx-auto max-w-2xl"><section className="mt-10 rounded-[26px] border border-border bg-surface p-6 sm:p-8"><Clipboard className="text-primary" /><h1 className="mt-3 font-display text-4xl">Tra cứu đơn hàng</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Nhập mã đơn và số điện thoại người nhận để xem nhanh trạng thái. Hệ thống chỉ hiển thị thông tin tối thiểu để bảo vệ đơn hàng.</p><form onSubmit={submit} className="mt-7 grid gap-4 sm:grid-cols-[1fr_1fr_auto] sm:items-end"><label className="text-sm font-semibold">Mã đơn<input value={orderCode} onChange={(event) => setOrderCode(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-border bg-background px-4 uppercase" placeholder="CSH-2026-..." required /></label><label className="text-sm font-semibold">Số điện thoại<input value={phone} onChange={(event) => setPhone(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-border bg-background px-4" inputMode="tel" placeholder="0356 925 367" required /></label><button disabled={loading} className="flex h-12 items-center justify-center gap-2 rounded-full bg-primary px-5 font-bold text-white disabled:opacity-60">{loading ? <LoaderCircle className="animate-spin" size={17} /> : <Search size={17} />} Tra cứu</button></form>{error && <p role="alert" className="mt-5 rounded-xl bg-[#fae8e4] p-4 text-sm text-danger">{error}</p>}{result && <article className="mt-7 rounded-2xl border border-border bg-background p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-bold">{result.order_code}</p><p className="mt-1 text-xs text-muted-foreground">{new Date(result.created_at).toLocaleString("vi-VN")}</p></div><span className="rounded-full bg-[#e4ecdf] px-3 py-1 text-xs font-bold text-primary">{statusLabels[result.status] || result.status}</span></div><p className="mt-5 text-sm"><strong>Người nhận:</strong> {result.recipient_name}</p><p className="mt-1 text-sm"><strong>{result.is_pickup ? "Nhận hoa:" : "Địa chỉ:"}</strong> {result.is_pickup ? "Tự tới lấy tại shop" : result.delivery_address}</p>{result.delivery_date && <p className="mt-1 text-sm"><strong>Thời gian:</strong> {result.delivery_date}{result.delivery_time ? ` · ${result.delivery_time}` : ""}</p>}<p className="mt-1 text-sm"><strong>Giao hàng:</strong> {deliveryLabels[result.delivery_status] || result.delivery_status}{result.carrier_name ? ` · ${result.carrier_name}` : ""}</p><div className="mt-5 space-y-2 border-t border-border pt-4 text-sm">{result.order_items.map((item, index) => <div key={index} className="flex justify-between gap-4"><span>{item.product_name_snapshot} × {item.quantity}</span><strong>{money(item.line_total_vnd)}đ</strong></div>)}<div className="mt-3 space-y-1 border-t border-border pt-3"><div className="flex justify-between"><span>Tạm tính</span><strong>{money(result.subtotal_vnd)}đ</strong></div><div className="flex justify-between"><span>Phí giao hàng</span><strong>{result.shipping_fee_confirmed || result.is_pickup ? `${money(result.shipping_vnd)}đ` : "Shop xác nhận sau"}</strong></div><div className="flex justify-between text-base"><strong>Tổng đơn</strong><strong className="text-primary">{money(result.total_vnd)}đ</strong></div><div className="flex justify-between"><span>Đã cọc</span><strong>{money(result.deposit_paid_vnd || 0)}đ</strong></div><div className="flex justify-between"><span>Còn lại</span><strong>{money(result.remaining_amount_vnd ?? result.total_vnd)}đ</strong></div><div className="flex justify-between text-xs text-muted-foreground"><span>Thanh toán</span><span>{paymentLabels[result.payment_status] || result.payment_status}</span></div></div></div></article>}</section><Link href="/" className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-primary"><ArrowLeft size={15} /> Về cửa hàng</Link></div></main>;
}
